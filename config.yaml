seed: 42

data:
  n_samples: 1000
  train_ratio: 0.8
  batch_size: 32

model:
  input_dim: 1
  hidden_dim: 16
  output_dim: 1

training:
  epochs: 50
  learning_rate: 0.001
  optimizer: "adam"

checkpoint:
  save_path: "checkpoints/best_model.pt"